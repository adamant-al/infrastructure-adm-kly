On change /etc/hostname - rebuild/restart some containers (check files)  

On change password - change in all prometheus use  

On Grafana deploy - set Prometheus basic auth in datasource

tasks:  
alertmanager  
traefik
